"use client";

import { useEffect } from "react";
import "bootstrap/dist/js/bootstrap.bundle.min.js";

export default function BootstrapClient() {
  // No UI, it just loads Bootstrap JS in the browser
  useEffect(() => {}, []);
  return null;
}